#include "MyArray.h"

/*template<class T>
MyArray<T>::MyArray()
{
	_vector = new std::vector<T>();
}

template<class T>
MyArray<T>::~MyArray()
{
	delete _vector;
}

template<class T>
void MyArray<T>::insert(T element)
{
	_vector.insert(element);
}

template<class T>
T MyArray<T>::operator[](int index)
{
	return _vector[index];
}

template<class T>
void MyArray<T>::pop(int index)
{
	_vector.pop_back();
}*/